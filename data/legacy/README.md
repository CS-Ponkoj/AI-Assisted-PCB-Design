# Legacy Data Files

These files are archived from the earlier prototype shape.

The current app uses:

- `data/sensors/<sensor>/sensor.json` for sensor definitions
- `data/board_template.json` for fixed board template data
- `data/requirement_keywords.json` for global parser settings

Do not edit these legacy files for new sensors.
